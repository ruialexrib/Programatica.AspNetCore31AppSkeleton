<script type="text/javascript">

    var audits = audits || {};

    audits.get_grid = () => $("#GridAudits")[0].ej2_instances[0];

    audits.on_grid_dblclick = (args) => {
        var id = args.rowData.Id;
        alert(id);
    }

    audits.show_modelaudit_view = (systemId) => {
        var url = "@Url.Action("ModelAudit", "Audits")?systemid=" + systemId;
        modal.show(url, modalsizes.LARGE)
            .then((h) => {
                debug("modal show event ok");
            })
            .catch((e) => {
                global.toasterror(e);
            });
    }

    audits.close_view = (e) => {
        e.preventDefault();
        modal.close(e);
    };

</script>
